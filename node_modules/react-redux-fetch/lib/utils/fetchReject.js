'use strict';

exports.__esModule = true;

var _request = require('../constants/request');

var fetchFulfill = function fetchFulfill(state, action) {
  return state.merge(_request.REJECTED).setIn(['reason'], action.reason).setIn(['meta'], action.request.meta).setIn(['meta', 'status'], action.request.meta.response && action.request.meta.response.status);
};

exports.default = fetchFulfill;